# EduMap IA — Dependências Python
# Instale com: pip install -r requirements.txt

# OCR e extração de texto
pymupdf>=1.23.0        # Extração de texto de PDFs digitais
pdfplumber>=0.10.0     # Fallback para PDFs complexos
pytesseract>=0.3.10    # OCR para imagens (requer Tesseract instalado no sistema)
Pillow>=10.0.0         # Processamento de imagens

# API REST
fastapi>=0.110.0       # Framework da API
uvicorn>=0.27.0        # Servidor ASGI
python-multipart>=0.0.9  # Upload de arquivos no FastAPI

# Banco de dados
psycopg2-binary>=2.9.0   # Driver PostgreSQL (produção)

# Autenticação
passlib[bcrypt]>=1.7.4        # Hash de senhas com bcrypt
python-jose[cryptography]>=3.3.0  # JWT

# Utilitários
fpdf2>=2.7.0           # Geração de PDFs (scripts de exemplo)
pandas>=2.0.0          # Manipulação de dados (opcional)
